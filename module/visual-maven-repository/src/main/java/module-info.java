module org.visual.maven.repository {
  requires org.apache.maven.resolver;
  requires org.apache.maven.resolver.impl;
  requires com.google.guice;
  requires jakarta.inject;
  requires static lombok;
}
