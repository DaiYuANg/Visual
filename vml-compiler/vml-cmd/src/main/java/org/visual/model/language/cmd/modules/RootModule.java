package org.visual.model.language.cmd.modules;

import com.google.inject.AbstractModule;

public class RootModule extends AbstractModule {
    @Override
    protected void configure() {
        super.configure();
    }
}
